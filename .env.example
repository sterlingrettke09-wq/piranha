# Anthropic API key for Claude calls in the /api/ask and /api/analyze functions.
# Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Mapbox access token (optional). Only needed for routes that render maps.
# Get one at https://account.mapbox.com/access-tokens/
MAPBOX_TOKEN=
